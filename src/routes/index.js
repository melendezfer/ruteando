const { Router } = require('express');

const router = Router();

// Cada épica monta aquí su propio router, con prefijo igual al de
// openapi.yaml (ej. router.use('/auth', require('./auth.routes')) en la
// Épica 1). Vacío intencionalmente hasta que exista lógica de negocio.

module.exports = router;
